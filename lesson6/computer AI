computer AI

can it win?
  scan win lines for 2 computer markers in a line - return empty square
need to block
  scan win lines for 2 player markers in a line - return empty square
take square 5

a = [1,2,3]
def scan_win_lines (key)
key_count = 0
other_count = 0
empty_square = 0
a.each {|i| brd[i] == key ? key_counter +=1 }
a.each {|i| brd[i] == ' ' && empty_square == 0 ? empty_square = i } #identify first empty sqaure
return key_count, other_count, empty_square
end

cntr = 0
square = 0
a.each {|i| brd[i] == player[:marker] ? cntr+=1 }
a.each {|i| brd[i] == ' ' ? square = i }
if cntr == 2
  return square
end

if brd[5] == ' '
  #take square 5
end

#random
  